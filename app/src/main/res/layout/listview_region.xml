<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="regionWrapper"
            type="org.unimelb.itime.ui.viewmodel.SettingRegionViewModel.RegionWrapper"/>
        <import type="android.view.View"/>
    </data>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:paddingStart="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:textSize="17sp"
        android:textColor="@color/black"
        android:text="@{regionWrapper.region.name}"/>

    <TextView
        android:id="@+id/region_indicator"
        style="@style/arrow_disclosure_indicator" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:visibility="@{regionWrapper.isSelected==1 ? View.VISIBLE : View.INVISIBLE}"
        android:text="@string/selected"
        android:textSize="15sp"
        android:textColor="@color/color_757575"
        android:layout_toStartOf="@+id/region_indicator"
        android:layout_marginEnd="10dp"/>


</RelativeLayout>
</layout>
