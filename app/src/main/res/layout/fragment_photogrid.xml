<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="org.unimelb.itime.ui.viewmodel.event.PhotoGridViewModel" />

        <variable
            name="toolbarVM"
            type="org.unimelb.itime.ui.viewmodel.ToolbarViewModel"/>
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/white">

        <include layout="@layout/toolbar_common"
            bind:vm="@{toolbarVM}"/>

        <org.unimelb.itime.widget.PhotoGridView
            android:id="@+id/gridview_photo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:photos = "@{viewModel.photos}"
            app:spacing="4dp"
            app:editable="@{viewModel.editable}"
            app:maxnum="@{viewModel.maxNum}"
            app:addMoreOnClickListener="@{viewModel.addMoreOnClickListener}"
            app:itemOnClickListener="@{viewModel.itemOnClickListener}"/>

    </LinearLayout>
</layout>
